# 更新日志

本项目的所有显著变更都会记录在此文件中。

维护约定（面向用户视角，不照抄 commit）：

- 每合并一个功能 / 修复，就在 `[未发布]` 区按分类补一条用户能读懂的描述；
- 发版时把 `[未发布]` 标题改成 `[版本号] - 日期`，删减润色后同步到 GitHub Release；
- 分类：`新增` / `优化` / `修复` / `部署与其他` / `升级说明`。

### commit 约定

- 标题一行，格式 `type: 中文描述`；type 用 `feat` / `fix` / `refactor` / `style` / `perf` / `docs` / `chore`。
- **正文保持精简：一般 5~8 行，不超过 15 行。** 只写「为什么这么改」与必要的影响面，
  不贴大段代码、不复述 diff、不逐条列举每个文件改了什么。
- 根因、验证方式这类信息写进 commit 只会被埋没——放代码注释或本文件里更合适。
- 一个 commit 只做一件事；修复与重构不要混在一起，方便回溯与回滚。

## [未发布]

### 升级说明

- **项目更名为 SeedArk**：原名 trpanel 已不再贴切——它早已不只管 Transmission，还能接 qBittorrent 并聚合展示。仓库、镜像、二进制与界面标题统一改为 SeedArk（镜像 `sushazhi/seedark`、`jianhuayanyu/seedark`）。**升级需注意**：环境变量前缀由 `TR_` / `TM_` 改为 `SA_`（如 `TR_URL` → `SA_URL`、`TM_DATA_DIR` → `SA_DATA_DIR`），状态文件由 `tm-state.json` 改为 `sa-state.json`，数据目录默认由 `~/.trpanel` 改为 `~/.seedark`。老部署请按新变量名调整启动参数，并把旧状态文件与连接配置迁到新目录，否则需要重新配置连接与策略

### 新增

- **qBittorrent 支持**：新增 qBittorrent 驱动（Web API v2，兼容 4.x / 5.x，支持 5.2+ API Key 登录），可与 Transmission 同时接入并聚合展示（种子列表合并、批量操作按种子自动路由到所属下载器、统计与站点维度跨服务器汇总）
- **qBittorrent 设置面板**：完整对齐官方 WebUI 的偏好项（行为 / 下载 / 连接 / 速度 / BitTorrent / RSS / WebUI / 高级），由驱动自述分节与字段、界面通用渲染，上游增删偏好键无需改界面；改动经吸顶条一次性保存，只提交改动项，危险项带提示
- **下载器设置按服务器切换**：多服务器下顶部一台一个标签，切换即读回该台自己的设置，读写互不影响；固定为「连接哪个显示哪个」——连 qBittorrent 显示 qB 面板并隐藏端口测试 / 黑名单 / 脚本钩子等用不上的入口，连 Transmission 保持原有表单
- **种子归属可见**：聚合视图下每颗种子标明属于哪台下载器。新增「下载器」列（默认隐藏，进入聚合视图时自动开启一次，之后由你自行控制；表头右键可随时开关或调整位置），用下载器类型与服务器名呈现，一眼分清合并列表里哪些在 Transmission、哪些在 qBittorrent；移动端卡片、种子详情同样显示归属。该列支持按服务器名排序。服务器名已以 QB / TR 打头时（如「QB-甲」）只显示名字并按其下载器着色，不重复挂类型徽标；名字看不出类型时才补一枚徽标

### 优化

- **设置弹窗改为桌面双列布局**：宽屏（≥768px）左侧常驻节导航、右侧只显示当前这一节，不再是一条从「连接配置」拉到「关于」的长滚动——qBittorrent 的「高级」一节有 65 个偏好项、展开后长达 3135px，此前翻到下一节要一路滚过它；内容超高时改为面板内滚动，弹窗高度恒定不再被撑长。窄屏（<768px）保持原有单列长滚动 + 折叠分节，移动端操作方式完全不变
- **「界面设置」拆出「外观与无障碍」**：壁纸、玻璃浓度与三档无障碍降级原先是同一节里最长的部分，独立成节后设置项定位更快。节名不再用「液态玻璃无障碍」——那是拿内部材质（Liquid Glass）命名，用户在这一节里管的是"界面看起来怎么样"
- **桌面端不再多套一层**：左栏导航已经写着节名，右栏原先还套一层同名的可折叠卡片——标题重复两遍、多一条边框，而且要点开 summary 才能看到内容。现在桌面端只给内容，窄屏仍保留折叠分节（那层外框是移动端唯一的定位手段）
- **连接地址直接填默认值**：Transmission 填 `http://localhost:9091/transmission/rpc`、qBittorrent 填 `http://localhost:8080`，不再只当灰色提示让你对着空框猜端口。切换下载器类型时，若地址还是空的或仍是另一类的默认值，会跟着换成新类型的；已保存的真实地址一律不动。多服务器管理里新增行同样预填
- **qBittorrent 自述面板按节分组导航**：驱动自述的 8 个分节（行为 / 下载 / 连接 / 速度 / BitTorrent / RSS / WebUI / 高级）各自成为导航项并配图标；单节内只读与危险项数超过阈值时自动收进「只读与危险项」折叠组。改动的草稿跨节保留，翻页后仍可一次性保存
- **MCP 工具清单随下载器自适应**：连接 qBittorrent 时不再列出用不了的工具——`transmission_api_request`（Transmission RPC 透传）、`update_blocklist`、`test_port` 三项从清单中消失，其余工具的描述会补上该下载器下的行为差异（如 qBittorrent 只提供全局下载目录的剩余空间、忽略「遵循全局限速」、移动种子必须连文件一起搬）；工具定义按下载器缓存，切换服务器后自动按新能力重建
- **设置面板去下载器特判，改为自述驱动**：此前设置面板靠「它是不是 qBittorrent」这个字符串判断该渲染哪套界面，接入新下载器要改散落的六处分支（默认地址表、两处重复的类型下拉、显示名与短标的多处三元判断）。现在判据换成**数据特征**——驱动给了字段自述（schema）就按自述通用渲染，没给就回落到内置表单；分节图标也改由驱动声明语义名（`speed` / `webui` …），前端不再维护「qB 的 behavior 节配哪个图标」的对照表。下载器的默认地址 / 显示名 / 短标收敛到一张注册表。**接入新下载器时前端只需加一行注册表**，若能复用既有字段类型则连渲染层都不用动
- **会话偏好通道更名 `qb` → `prefs`**：该字段本就是「驱动自己的原生配置键值对」，叫 `qb` 会在接入第二个自述驱动时名不副实（Transmission 的 `session-*` 键总不能塞进 qb 字段）。请求与响应统一改走 `prefs`，**旧的 `qb` 键仍被受理**（老前端不发新版也能用）

### 新增

- **侧边栏「下载器」分布筛选**：多下载器聚合时（≥2 台），左侧栏新增一组按下载器分组的筛选，每台显示 TR / QB 徽标、名称、种子数与合计体积，点一下只看这台、可多选叠加，双击可全选该组，右键菜单可整组隐藏。**只在聚合视图出现**——单台部署下它是个恒真的筛选，没有意义；没有任何种子的下载器也不会占位。筛选条件按**服务器名**而非索引记录，服务器列表增删重排后不会串到另一台

### 修复

- **设置面板读不到某台服务器时提示说的是人话**：多服务器下若活动连接与服务器列表里的那条记录对不上（切换时写盘失败、或改过 `.env` 后没重启等），打开该服务器的设置会失败——后端给的原因本是「当前连接与「xxx」不一致，请重新切换到该服务器」，但界面直接渲染了 axios 的 `Request failed with status code 400`，把能指导下一步动作的那句吞掉了。现在优先取响应体里的 message，并在这类失配下额外给出「重新切换到该服务器」按钮，一键即可恢复（此前只能对着一句报错反复点同一个标签）
- **打开设置弹窗可能触发无限渲染**：qBittorrent 设置草稿的丢弃逻辑以 `values` 对象本身为 effect 依赖，而调用方写的是 `session?.qb ?? {}`——会话里没有 `qb` 字段时（连接的是 Transmission、或 qB 会话尚未读回）每次渲染都会新建一个空对象，effect 遂每轮都跑，`setDraft({})` 再触发下一轮，控制台刷满 `Maximum update depth exceeded`。改为依赖取值的指纹
- **聚合视图下打不开「当前服务器」的 qBittorrent 种子详情**：多下载器聚合时，打开列表里属于当前连接那台的种子详情会报「种子不存在」（qBittorrent 面板 ID 是与驱动实例绑定的内存映射，而聚合列表与详情曾分别走两个不同的驱动实例，用甲实例分配的 ID 去乙实例查必然落空）；非当前服务器的种子不受影响，因此现象看起来像「只有一台打不开」。现在按 ID 寻址的操作统一路由到与聚合列表同一个实例

## [v0.1.2] - 2026-09-19

### 新增

- **Peer 归属地开箱即用**：启动时自动从镜像下载 GeoIP 数据库到数据目录（仅首次，约 60MB，后台进行不阻塞服务），无需再手动放入 `GeoLite2-City.mmdb`；手动放置的文件仍优先使用

### 优化

- **打开应用秒开（大库优化）**：真机定位到「站点过滤」接口拖慢了整体首屏——它此前向 Transmission 拉取全部种子的全量字段（含文件列表 / Peer / 块位图），590 个种子时仅这一个请求就要约 9 秒，应用打开时与列表并发抢占资源，列表迟迟出不来；现在只请求站点归属所需的两个轻量字段，同一环境实测降到 0.1 秒内。替换 Tracker 功能的取数方式一并修正（此前同样全量拉取，同样受益）
- **界面一致性**：表格数字列的表头改为与内容同侧对齐（此前表头居中、数值右对齐，纵向扫视对不上）；排序箭头改为常驻槽位，切换排序时表头不再左右跳动
- **顶栏更清爽**：创建种子、打开下载目录、配置目录收进「⋯」溢出菜单，常驻图标从 9 颗减到 6 颗，「添加任务」成为唯一视觉落点
- **移动端状态栏**：底栏不再挤成上下两行（此前窄屏会换行并被定高容器裁掉）；会话流量紧跟在「已连接」之后，窄屏只隐藏文字标签、保留 ↓/↑ 数字；完整明细收进「详情」浮层
- **材质统一**：设置与统计弹窗内的分区块、规则行改用新的玻璃材质档，不再出现贴在玻璃上的灰色硬边；阴影全部回归令牌
- **移动端**：列表顶部「全部 (N)」标题可点开状态分组菜单（与侧边栏同口径）；悬浮工具胶囊新增「设置」入口、「添加任务」换幼苗图标；触屏选择框改为圆形
- **动效对齐 iOS**：颜色 / 透明度过渡统一缓动曲线，长按菜单从按压位置弹出，开关与顶栏圆形按钮缩放改用弹簧曲线
- **桌面表格**横向滚动条挪到列表底部并加粗，支持拖拽 / 点击跳转（无横向溢出时自动隐藏）
- **种子详情**：「用户」列表的「国家/地区」列移到「地址」左侧，先看归属地再看 IP
- **添加种子**：「下载目录」在飞牛环境下新增宿主目录选择器，不用再手输 NAS 路径（浏览器等环境不显示该入口，仍可手输或从历史记录选择）
- 多服务器列表保存改为防抖提交，不再逐字符提交
- **MCP 接入信息只在真能用时展示**：未启用或缺少直连端口时不再给出地址与配置 JSON，改为提示先补全配置

### 修复

- **检查更新的「更新日志」不再显示 `<br>` 乱格式**：应用中心风格的 HTML 更新日志（`<br>` 分行）经 Release 原样下发，弹窗里会原样显示标签且不换行；现在后端统一转为纯文本换行并去掉冗余标题行
- **移动端「列表一闪之后整页空白」**（根因修复）：卡片列表把 `useTorrentMenuHandler`（自定义 Hook，内部含 4 个 Hook）写在了 `torrents.length === 0` 的提前 return 之后。首屏列表为空时走空态分支、Hook 调用数偏少；接口返回种子后重渲染多出 Hook，React 抛 #310（Rendered more hooks than during the previous render）并卸载整棵树 → 白屏。移动端固定走卡片视图、桌面走表格，所以该崩溃只在移动端出现，且表现为「闪一下就没」。已把该 Hook 提到提前 return 之前，Hook 顺序在空/非空两种状态下保持一致
- **静态资源回退与 SW 缓存加固**（纵深防御，避免同类白屏再次被固化）：后端 SPA 回退不再对已删除的 `/assets/*.js` 返回 200 + HTML，构建产物路径未命中一律返回真 404；Service Worker 缓存名改为随构建指纹变化（激活时清理旧版本），且只缓存内容类型与请求相符的成功响应，杜绝 HTML 被当作脚本长期缓存。已被历史版本污染的客户端会在下次打开时自动检测并清理一次
- **安全**：修复保存连接配置漏写 MCP 项导致「切换服务器」清空接入令牌、重启后 MCP 端点无鉴权的问题；直连端口现在必须配合令牌才启动（保存时拒绝「端口非空 + 令牌为空」，运行期清空令牌即拒绝请求）；「添加种子」仅接受 http(s) 与磁力链接
- 修复删除「当前服务器」后仍连着已删除服务器、以及编辑服务器地址 / 名称后密码被静默清空的问题
- 修复批量添加时「文件勾选」可能套用到另一种子、链接添加失败后重复添加的问题
- 修复切换 / 删除服务器或服务端推送删除种子后旧选中项残留、批量操作作用到错误种子的问题
- **修复筛选条件残留导致种子列表长期空白**：侧栏「错误分布」等条件失效后列表永远空白，现在筛选标签行内可直接移除，空列表也区分「暂无数据」与「没有匹配的种子」并给出「清除全部筛选」入口
- 修复接口返回非预期内容（如宿主网关登录态失效返回 `invalid token`）时只表现为「列表空白 + 笼统失败」，现在会提示重新登录
- 修复「轮询间隔」对 WebSocket 不可用环境的兜底轮询无效、以及批量「开始」后列表不立即刷新的问题
- 修复未按「队列位置」排序时拖拽排序看似无效却仍发出队列调整请求的问题，现在会提示先按队列位置排序
- 修复删除种子后确认框不自动关闭、设置面板连续快速切换开关时前一次改动被覆盖的问题
- 修复侧边栏「站点分布」分组较多时被面板底部裁切无法下滑的问题；「活跃」分组改为只统计实际有流量的种子
- 修复滚动条迟迟才出现（新版 Chrome/Edge 回退到系统悬浮滚动条）的问题，恢复常驻细滚动条；侧栏磁盘「可用 / 总容量」改同行显示
- 修复种子详情「用户」列表「国家/地区」显示异常：此前依赖系统国旗 emoji，Windows 缺少对应字体时会退化成重复字母（如「cn CN」）；现改用自托管 SVG 国旗 + 本地化国家名（如「🇨🇳 中国」），Windows / macOS / 手机显示一致，无归属地时显示地球占位；同时修正在线演示返回国家全名、与真实后端（ISO 代码）不一致的问题
- 修复 fnOS 下「数据目录」等处不显示语义化路径的问题，兼容网关返回的数组 / 对象两种格式
- GeoIP 数据库自动下载补齐超时与体积上限，避免镜像异常拖住后台任务；修复后端建种任务进度查询的并发读写问题

### 部署与其他

- 镜像同步发布到 Docker Hub（`jianhuayanyu/trpanel`，国内拉取更快），与 GHCR 内容完全一致；README 与 docker-compose 默认镜像相应更新

## [0.1.1] - 2026-09-06

### 新增

- **MCP 直连端口可配置**：设置页可填写 / 清空 AI 客户端直连端口（1-65535 校验），留空即关闭并回显主服务地址；修改需重启后端生效（监听器随进程启动绑定）
- **MCP 接入信息一键复制**：MCP 设置面板提供「复制连接地址」「复制配置 JSON」，中英文文案同步；复制失败（如内嵌预览未授予剪贴板权限）会明确提示，而不是静默失败
- **分组限速**：把同一站点 / 标签 / 名称下的种子视为一组，为整组分别设定下载与上传的总上限（0 = 该方向不限）。引擎定期把「上限 − 组内已单独限速种子占用」后的余量均分给组内运行中的种子，使整组合计速度不超过上限；你单独设置过的限速与「跟随全局限速」标记不会被覆盖，删除规则或关闭引擎时自动还原。
  - 入口：设置 → 自动化 → 分组限速；侧边栏站点 / 标签项右键 →「为此分组添加限速规则」（自动预填范围）
  - 支持手动「立即应用」，并回显接管 / 下发 / 还原统计
- **批量限速**：选中多个种子后，顶栏批量工具条新增限速按钮，可统一设置或解除所选种子的下载 / 上传限速与连接数上限
- **内置 MCP 服务**：通过 `/mcp` 端点以工具形式暴露种子管理能力，AI 客户端（Claude、Qoder 等）可直接查询、添加、暂停 / 启动种子；支持接入令牌鉴权与「允许删除」开关，设置页修改即时生效
- **MCP 通用透传工具** `transmission_api_request`：可调用 Transmission 官方 RPC 白名单方法（覆盖客户端库未封装的能力）；只读方法直接可用，写方法受「允许通过 MCP 执行高危操作」开关管控，关停会话与宿主机脚本类敏感配置一律屏蔽
- **MCP 专用直连端口**：飞牛部署下可开启独立 TCP 端口（`MCP_PORT`，fpk 默认 9094）只暴露 `/mcp`，外部 AI 客户端无需经过统一网关即可接入；设置页展示直连地址
- **在线演示**：GitHub Pages 提供在线预览（推送到 master 自动构建部署），数据在浏览器内模拟生成、可交互（启停 / 删除 / 添加 / 设置均生效），界面右上角显示「演示模式」徽标；`/` 为电脑版，`/mobile.html` 为手机壳样式的移动版预览（真机打开自动进入应用本体）

### 优化

- 侧栏分组更清晰：目录分布更名为「数据目录」、标签分布更名为「种子标签」，种子标签下新增「全部 / 无标签」入口，顶栏激活筛选条同步识别「无标签」
- 底部状态栏「活跃 / 暂停 / 校验」计数改用与侧栏过滤器同一口径，两处数字不再对不上
- WebSocket 全量推送只做一次序列化，降低大列表下的 CPU 与内存峰值
- 做种策略预览复用状态快照，减少每轮评估的深拷贝开销
- 表格视图支持横向滚动（列总宽可超出视口），滚动条不再被玻璃渐隐遮住
- 列表列「下载 / 上传」更名为「下载速度 / 上传速度」，避免与限速值混淆

### 修复

- 单种子限速弹窗文案纠正：最大连接数填 **0 表示不连接任何 Peer**（并非不限），输入最小值改为 1；留空表示不修改
- 移动端 WebView 布局修复

### 部署与其他

- 新增 Dockerfile 与 CI，发布时自动构建多架构镜像
- 开发脚本 `dev.ps1` / `dev.sh`：后端支持 air 热重载，前端增加 HMR 就绪探测，避免拿到不热更的旧实例
- 项目协议变更为 MIT

### 升级说明

- 状态文件自动兼容：新增 `speedPolicy*` 字段，旧 `tm-state.json` 无需任何改动
- 分组限速引擎默认关闭，升级后需在 设置 → 自动化 → 分组限速 中手动开启

## [v0.1.0] - 2026-08-31

### 新增

- Transmission 管理面板核心：种子列表 / 详情 / 筛选排序 / 批量操作 / 添加种子
- 做种策略引擎：按站点设定分享率 / 做种时长 / 上传量目标，达标后自动暂停或删除
- 背景壁纸与玻璃折射、顶栏模糊搜索、问题反馈入口
- 内置 Transmission 开发 mock（`cmd/trmock`）、应用更新提示、活跃分类与选中态强化
- 五档布局分区、访问令牌输入、子路径 PWA

### 优化

- 飞牛（fnOS）专属特性抽象为宿主平台层，同一份代码支持通用部署
- 玻璃浓度调节，设置-关于文字清晰度提升

### 修复

- 收紧鉴权、路径读取与配置写入的安全边界
- 网关模式下信任宿主认证，放宽同源校验

[v0.1.2]: https://github.com/sushazhi/trpanel/compare/v0.1.1...v0.1.2
[0.1.1]: https://github.com/sushazhi/trpanel/releases/tag/v0.1.1
[v0.1.0]: https://github.com/sushazhi/trpanel/releases/tag/v0.1.0
